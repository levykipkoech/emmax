import { Fugaz_One, Open_Sans, Rubik_Wet_Paint } from 'next/font/google';
import "./globals.css";
import Navbar from './components/Navbar';


const opensans = Open_Sans({ subsets: ['latin'] });
const fugaz = Fugaz_One({ subsets: ['latin'], weight: ['400']});
const rubik = Rubik_Wet_Paint({ subsets: ['latin'], weight: ['400']});

export const metadata = {
  title: "emmax",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
 const header = (
  <header className="flex pt-3">
    <Navbar/>
  </header>
 )
 const footer = (
  <footer>
    <p>footer</p>
  </footer>
 )
  return (
    <html lang="en">
     
      <body
        className={"w-full mx-auto text-sm bg-[#2F4146] text-[#F5EFEB] sm:text-base min-h-screen " + opensans.className}
      >
        {header}
        {children}
        {footer}
      </body>
     
    </html>
  );
}
